# toggl-report
Console app em C#/.NET que gera relatórios de tempo trabalhado via API do Toggl Track, agrupando por descrição, tag e usuário em um período. Wizard interativo configura tokens e período, salvando em INI. Exporta CSV e permite buscar por parte da descrição, exibindo o resultado em tabela por usuário.
