# Copie para .env (gitignored) e preencha com a senha real do certificado
# usado em ./certificado/certificado.pfx (docker-compose.yml, serviço
# toggl_report_api). O docker-compose lê o .env da raiz automaticamente.
KESTREL_CERT_PASSWORD=